---
interface Props {
  title: string;
  date: Date;
  description: string;
  slug: string;
}

const { title, date, description, slug } = Astro.props;

const formattedDate = date.toLocaleDateString('en-US', {
  month: 'short',
  year: 'numeric',
});
---

<a href={`/writing/${slug}/`} class="group block p-8 hover:bg-white article-hover">
  <time class="font-mono text-xs uppercase tracking-wider block mb-4 text-bark">
    {formattedDate}
  </time>
  <h2 class="text-3xl font-light mb-4 group-hover:opacity-70 transition text-forest">
    {title}
  </h2>
  <p class="text-lg leading-relaxed mb-6 text-sage">
    {description}
  </p>
  <span class="font-mono text-sm arrow-link text-moss">
    → read article
  </span>
</a>
